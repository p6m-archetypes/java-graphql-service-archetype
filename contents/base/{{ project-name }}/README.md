# {{ project_title }}

GraphQL service generated by `java-graphql-service-archetype`.

## Build

```bash
mvn verify
```

Service on port `{{ service_port }}`, management on `{{ management_port }}`. GraphiQL at `http://localhost:{{ service_port }}/graphiql`.
